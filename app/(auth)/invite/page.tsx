import SetPassword from "@/components/set-password";
import { NextPage } from "next";

const VerifiedSignUp: NextPage = () => {
  return <SetPassword type="INVITE" />;
};

export default VerifiedSignUp;
